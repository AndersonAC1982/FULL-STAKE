//CÓDIGO NÃO EXECUTÁVEL
// Objeto é uma coleção de propriedades, onde cada propriedade é uma associação entre uma chave (string) e um valor (qualquer tipo de dado).

//PROCEDURAL
processamento = (valor1, valor2, valor3)

//Objeto
objeto = {
    valor1,
    valor2,
    valor3,
    processamento()  {
        //logica de processamento

    }
     
}

objeto.processamento() //chamada do método

// Objeto é uma instância de uma classe, que define um modelo para criar objetos com propriedades e métodos.
//principios importantes da POO
// 1. Abstração: ocultar detalhes complexos e mostrar apenas o necessário.
// 2. Encapsulamento: agrupar dados e comportamentos relacionados em uma única unidade (objeto).
// 3. Herança: permitir que uma classe herde propriedades e métodos de outra classe.
// 4. Polimorfismo: permitir que diferentes classes implementem métodos com o mesmo nome, mas comportamentos diferentes.
